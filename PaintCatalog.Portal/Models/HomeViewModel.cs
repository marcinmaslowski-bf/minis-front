namespace PaintCatalog.Portal.Models
{
    public class HomeViewModel
    {
        /// <summary>
        /// Raw JSON returned from GET /api/v1/paints (sample for debugging / integration).
        /// </summary>
        public string? PaintsJsonSample { get; set; }
    }
}
